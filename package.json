{
  "name": "spire-api-server",
  "version": "1.0.0",
  "main": "server.js",
  "dependencies": {
    "axios": "^1.6.0",
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node test-spire.js",
    "build": "echo 'No build step required for Node.js'",
    "deploy": "echo 'Deploy to EC2 using your preferred method'"
  }
}
